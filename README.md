# calendar-alerts

Command line tool to generate weekly summaries of Google Calendar events.

Install dependencies with `uv sync`.

See options:

```
uv run main.py --help
```

Configuration is handled with environment variables or command line options. 